# Compiled Classes

This folder contains the compiled `.class` files generated from the source code.

## Usage:
When you compile the project, the `.class` files will be placed here.

## Compile Command:
```bash
javac -d bin -sourcepath src src/**/*.java
```

This folder is listed in `.gitignore` so compiled files are not committed to version control.
