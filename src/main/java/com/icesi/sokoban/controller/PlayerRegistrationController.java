package com.icesi.sokoban.controller;

import javafx.fxml.FXML;
import javafx.fxml.Initializable;

import java.net.URL;
import java.util.ResourceBundle;

/**
 * CONTROLLER — PlayerRegistrationController
 *
 * Controlador FXML para la pantalla de registro de jugador (RF1).
 *
 * ESQUELETO PENDIENTE POR IMPLEMENTAR (equipo):
 *   - Declarar los nodos @FXML que correspondan a la vista.
 *   - Implementar los metodos manejadores de eventos.
 *   - Conectar con el modelo correspondiente.
 */
public class PlayerRegistrationController implements Initializable {

    @Override
    public void initialize(URL location, ResourceBundle resources) {
        // TODO equipo: inicializar la pantalla de registro de jugador (RF1)
    }
}
